const names = document.getElementById('submit-btn')
const responseContainer = document.getElementById('response-container')

names.addEventListener('click', (e) => {
  e.preventDefault();

  computeUsersStats()


  /*let liobj =document.createElement('li');
      liobj.innerHTML = newUserWithStats;
      responseContainer.appendChild(liobj);
   //hacer un for para imprimir  el listado */

  //let printnewUserWiS = for (let printUserWiS of UserWithStats) {
    //return
//

  //responseContainer.appendChild(li);
});


